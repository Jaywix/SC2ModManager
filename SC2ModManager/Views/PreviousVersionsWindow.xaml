<Window x:Class="SC2ModManager.Views.PreviousVersionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Previous Versions"
        Height="680"
        Width="700"
        MinHeight="400"
        MinWidth="500"
        WindowStartupLocation="CenterOwner"
        Background="#0D1117"
        Foreground="White"
        FontFamily="Segoe UI"
        FontSize="14">

    <Window.Resources>
        <!-- Modern button style matching the main window -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1A2535"/>
            <Setter Property="BorderBrush" Value="#1E90FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E3550"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1270CC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#2A2A2A"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#444"/>
                                <Setter Property="Foreground" Value="#666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RestoreButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#1A2535"/>
            <Setter Property="BorderBrush" Value="#1E90FF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="140"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#161B22" Padding="20,14">
            <StackPanel>
                <TextBlock Text="Previous Versions"
                           FontSize="20" FontWeight="SemiBold" Foreground="White"/>
                <TextBlock Text="Restore a previous version of SC2 Mod Manager. Only versions 1.6.0 and newer are available."
                           FontSize="12" Foreground="#8B949E" Margin="0,4,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Warning banner -->
        <Border Grid.Row="1" Background="#1C1600" BorderBrush="#856404" BorderThickness="0,0,0,1" Padding="20,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠" Foreground="#E3B341" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock TextWrapping="Wrap" Foreground="#E3B341" FontSize="12" VerticalAlignment="Center">
                    <Run Text="Rolling back to an older version may cause mods to stop working and can cause issues with newer features."/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Version list -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="0,0,4,0">
            <StackPanel x:Name="VersionListPanel" Margin="16,12,16,12">
                <!-- Loading indicator -->
                <TextBlock x:Name="LoadingText"
                           Text="Loading versions..."
                           Foreground="#8B949E"
                           FontSize="13"
                           Margin="4,8"/>

                <!-- Error text -->
                <TextBlock x:Name="ErrorText"
                           Text=""
                           Foreground="#F85149"
                           FontSize="13"
                           Margin="4,8"
                           Visibility="Collapsed"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#161B22" Padding="20,12">
            <Button Content="Close"
                    Style="{StaticResource ModernButton}"
                    HorizontalAlignment="Right"
                    Width="90"
                    Click="Close_Click"/>
        </Border>
    </Grid>
</Window>
